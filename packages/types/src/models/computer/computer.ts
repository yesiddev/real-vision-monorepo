export interface Brand{
    uuid: string;
    name: string;
    slug: string;
    specs: string[]
    image: string;
    status: boolean;
    description: string;
    createdAt: string;
    updatedAt: string;
}